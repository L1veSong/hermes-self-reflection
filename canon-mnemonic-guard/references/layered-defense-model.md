# 双层防御模型 (Layered Defense Model)

## 背景

CMG SKILL.md 设计意图识别为「语义判断，非关键词匹配」。但 sentinel Plugin v1.0.0 只做了关键词子串匹配，导致大量口语化纠正漏过。

v1.1.0 引入双层模型解决此问题。

## 架构

```
用户消息
  ├─ A 层哨兵（sentinel Plugin · pre_llm_call）
  │   否定词正则广撒网 — 三组正则覆盖 80%+ 纠正句式
  │   → 命中 → 注入 [CMG-SENTINEL] suspected_correction
  │   → 未命中 → 放行
  │
  ├─ B 层判断（CMG Skill · LLM 语义理解）
  │   看到 [CMG-SENTINEL] 标记 → 优先执行意图识别
  │   → 确认是纠正 → 执行 CMG 三步走
  │   → 不是纠正 → 忽略标记，正常执行
  │
  └─ C 层硬拦截（sentinel Plugin · transform_llm_output）
      扫描 AI 输出 → 命中 ban 规则关键词 → 替换输出
```

## A 层正则（三组）

| 组 | 模式 | 覆盖 |
|----|------|------|
| 1 | 否定词 + {0,10} + 指代 | "你怎么又""别这么干" |
| 2 | 句首否定命令 | "别瞎说""不要这样" |
| 3 | 反问纠正/独立纠正信号 | "记住了吗""说了多少次""能不能" |

## 设计原则

- **A 层不做语义理解** — 只做正则，广撒网不漏
- **B 层不做正则匹配** — 只做语义，精准判断
- **A 不替代 B，B 不替代 A** — 哨兵 + 狙击手
- **Sentinel 默认 ON** — 零配置，安装即生效

## 覆盖率

v1.0.0 关键词匹配：~20% 纠正句式
v1.1.0 双层模型：~80% 纠正句式（A 层正则）+ 100%（B 层语义不设限）

## 相关规则

- rule_019: 用户纠错必须立刻固化规则（关键词已扩展至 18 个）
- rule_meta_001: 意图识别优先规则（B 层语义触发依据）
